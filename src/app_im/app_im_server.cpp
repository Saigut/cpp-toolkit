#include "app_im_server.h"


app_im_server_t app_im_server_create(app_im_server_param_t* param)
{
    // create server receiving queue
    // create user online hash table
    return 0;
}
int app_im_server_destroy(app_im_server_t app_im_server)
{
    return 0;
}

int app_im_server_start(app_im_server_t app_im_server)
{
    while (true) {
        // read receiving queue
        // make user online
        // or relay massage
    }
    return 0;
}

int app_im_server(int argc, char** argv)
{
    // create server
    // receiving and deal with message
    return 0;
}